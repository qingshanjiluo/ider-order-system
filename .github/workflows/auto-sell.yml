name: 一键卖�?

on:
  workflow_dispatch:
    inputs:
      username:
        description: '游戏账号（留空使用默�?zzhx�?
        required: false
        default: ''
        type: string
      password:
        description: '游戏密码'
        required: false
        default: ''
        type: string
      min_quality:
        description: '最低品质（1�?2�?3�?4�?5金）'
        required: true
        default: '1'
        type: number
      max_quality:
        description: '最高品�?
        required: true
        default: '3'
        type: number

jobs:
  auto-sell:
    name: 一键卖�?
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: 运行一键卖�?
        env:
          SELL_USERNAME: ${{ inputs.username || secrets.SELL_USERNAME }}
          SELL_PASSWORD: ${{ inputs.password || secrets.SELL_PASSWORD }}
          SELL_MIN_QUALITY: ${{ inputs.min_quality }}
          SELL_MAX_QUALITY: ${{ inputs.max_quality }}
        run: node gh-actions/auto_sell.js