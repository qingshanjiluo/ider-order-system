FROM node:24-alpine
ENV NODE_ENV=production
WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

COPY . .
RUN mkdir -p data backups logs && chown -R node:node /app

USER node
EXPOSE 3000
CMD ["node", "server.js"]
