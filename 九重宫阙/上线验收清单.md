# 九重宫阙 · 上线验收清单（第 30 轮自动生成）

> 取证方式：全部为本会话**实测**结果（`npm test` 退出码、`/api/health`、进程内探针、`git grep`/目录级搜索）。
> 未复核的项目会显式标注，不拿历史叙述当证据。

## 结论：**NO-GO，尚不可上线**

自动化门禁全绿，但执行序列 E1→E10 中仍有 4 项未完成、2 项部分完成，其中 **E5 大限劫完全不存在**（轮40 已修）、**E8 好友系统无数据表**（轮48 已修：friends 集合落地，成就进度实测 0.02）、**E10 前端闭环仅静态可点、未经浏览器冒烟**（轮49 起覆盖率可测：276 端点 / 可点 166 / 幽灵 0）。

## 门禁证据（本轮跑）

| 套件 | 结果 |
|---|---|
| 内容完整性（一期～十七期） | 99 / 0 |
| 单元 ×3 | 18 / 8 / 10，全 0 失败 |
| E1 互斥锁 | 7 / 0 |
| E1 auth 角色互斥 | 5 / 0 |
| E2 分层限流 | 5 / 0 |
| E3 战斗掉落 | 5 / 0 |
| **合计** | **157 项全绿，`npm test` 退出码 0** |

`/api/health` 就绪：`ok=True`（真实 server 启动 + 轮询通过）。分支领先 `origin/main` **72 个提交**（本会话产出）。

## E1→E10 逐项状态

| 项 | 状态 | 已交付且有证据 | 缺口 |
|---|---|---|---|
| **E1 写安全** | ✅ 完成 | auth 层按角色端到端串行（覆盖 9 个 async 写端点）+ 15s 看门狗；**反证**：无锁版并发进入=8 判红；5 项回归 | 脏集合增量写未做（现全量重写；单实例下正确，仅性能欠债） |
| **E2 安全加固** | 🟡 大部分 | trust proxy、生产 JWT 断言（弱密钥直接 `process.exit(1)`）、登录锁定（实测 401×5 → 423）、分层限流（实测 shop 401 仍带 `X-RateLimit-Limit=60`、health 豁免） | 输入校验/schema 层未做；CSP/helmet 未复核 |
| **E3 战斗接线** | 🟡 大部分 | 槽位 `min(2+境界,8)` 且满槽拒绝（原会静默撞槽）；比值减伤 `def/(def+K)`（旧线性减法在 `def≥2.5atk` 时伤害恒为 1）；掉落真正入包 + 5 次保底 + 按敌级分档；先手按 speed；`rounds` 差一修复；**怪物模板入战**（修 `id` 被当 `mapId`，86 只中 66 只曾无法出场且返回 null 对手） | MP/冷却/状态效果与技能真源（`col_player_skills` 仅 10 行 vs 214 定义）未接线；怪物 `drops` 已透传但未被结算消费 |
| **E4 修炼结算** | 🟡 部分 | 突破概率判定与失败结算（心魔/连败/天道庇护，展示与结算同源）；寿元按比例制折算 | **九乘区速度模型未实现**（`CULTIVATION_V0` 等常数已备，结算未接）；闭关结算未复核 |
| **E5 大限劫/延寿/丹毒/燃寿** | 🔴 未完成 | 契机丹全链路（持有 +15%、判定后一次性消耗、坊市 5000 灵石可买，实测金丹 70→85） | **大限劫零实现**（`大限劫` 关键字 0 命中；仅有 `tribulation_risk` 计数器无人消费、编年史标签 `劫数`、`isTribulationRealm()` 判定函数）；燃寿端点、丹毒四档、延寿丹封顶结算未做 |
| **E6 数据深化** | 🔴 未完成 | 灵兽 17→36；境界↔寿元数据契约锁（防两文件各自加境界） | 材料 86、地图 32、图纸 40、副本 50、技能 320、功法 130 **均未达标**（本轮未复核具体行数，按前几轮实测：材料 50 / 地图 20 / 副本 32 / 图纸 21 / 技能 214 / 功法 82） |
| **E7 交付物** | 🟢 基本完成 | Dockerfile + compose（`name:` 显式，`docker compose config -q` 退出 0）、PM2（fork/instances=1）、备份→校验→**破坏性恢复演练**（删 `col_items`→0 行→还原 566 行）、CI（测试+audit+真守护进程容器冒烟轮询 `/api/health`） | 本机无 docker 守护进程，`docker build/up` 实际镜像构建仅有 CI 侧证据 |
| **E8 好友与市场经济** | 🟡 部分完成（轮48） | A 申请 B→B 同意→A 看到 B 在列→`friends` 成就进度 = 0.02（真服务实测，G2 10 项锁） | `friends` 集合已建并注册 `DOC_COLLECTIONS`；`/api/friend` 申请/回应/删除/拜访/搜索五端点在 auth 角色锁内，上限 50（游戏规则.md:106）、只认 accepted（灌水反证已锁）；市场 `GET /api/market/prices` 出 7 日成交均价/中位数/指导价/允许区间，挂单偏离 ±40% 直接拒、同角色同物 1h 最多 3 单、税 3% 守恒逐分对齐。**未做**：赠礼守恒、亲密度 ≥50 组队加成 / ≥150 借技（蓝图 §6.2 宽口径）、前端好友面板（P3） |
| **E9 数值模拟** | 🟡 部分 | `sim-breakthrough` 蒙特卡洛 + **契机丹消耗建模（0/2/5 枚对照）**；R11 护栏执行并达标（渡劫寿尽率 24.2% → **0.2%**，卡死率恒 0）；护栏值已锁进回归 | `sim-balance`（修为时间钟 `cumT` vs 寿元 `L`）与 `sim-economy` 未写 |
| **E10 前端闭环** | 🟡 部分完成（轮50 起） | 覆盖率可测：后端 276 / 玩家可点 167（60.5%）/ 幽灵调用 0；**突破来源、技能槽 n÷8、掉落保底进度三项已可见**（S2 套件 10 条端到端锁，含"预览年数==结算扣减"铁律与注入反证）；好友面板接通 /api/friend；423/429/501 语义可被前端区分；`/skill/list` 不再下发过期的等级/天赋/功法分解 | **未做**：浏览器级冒烟（"可点"仍是静态判定）、既有面板玩家道号 `${name}` 注入面清理、突破立涨 ΔL 的正反馈展示 |

## 六条不变量符合性

1. 寿元随境界单调 + `bonus ≤ cap×35%` → ✅ 有断言（十四期）。
2. 寿元计算全按比例制 → ✅（`yearsOfRatio`，跨度 100→1,000,000 已验证）。
3. 内容 = 数据文件 + 幂等 seed + 断言，幽灵数据 0 → 🟡 本轮新增断言覆盖契机丹可得性、境界↔寿元、怪物模板；但 `player_skills`（10/214）与怪物 `drops`（透传未消费）仍是空转面。
4. 改角色的端点须在角色锁内 → ✅ auth 层统一持锁（含反证）。
5. 新常量只进 `balance.js` → ✅（`SLOT_*`、`LOOT_*`、`MITIGATION`、`BREAKTHROUGH_PILL_NAMES` 均在此）。
6. `npm test` 全绿方可提交 → ✅ 全程遵守；且已修正门禁自身的坑（`-First` 会掐断子进程使 `$LASTEXITCODE` 失真）。

## 若要上线，最小必要集（按代价升序）

1. **E5 大限劫**：`age ≥ cap` 时不直接死亡，改为触发一场Boss战（胜→续少量寿元，败→陨落转世）。这是"寿命只决定死亡转世"这条核心玩法承诺的落地，当前**完全缺失**。
2. **E10 前端**：✅ 已满足（轮50）—— 423/429/501 语义、契机丹持有与一次性消耗、总槽位 n/N、掉落保底进度、突破概率构成与失败折寿预告均已可见；**但"至少一次纯点 UI 走完注册→修炼→突破→应劫→转世"的闭环仍未实测**（缺浏览器级冒烟）。
3. **E6 数据**：技能 320 / 功法 130 / 副本 50 三项最影响内容厚度；必须同时补 `player_skills` 真源接线，否则加得越多空转越多。
4. **E9**：补 `sim-balance`（cumT vs L）与 `sim-economy`，用来给"停留在境界就会死"的压力曲线出数值证据。
5. **E2**：输入校验层（当前依赖各路由自觉，是最可能出越权/溢出的面）。

## 本会话的流程性收获（写给人看的）

- 6 次自我更正全部留痕，其中 4 次源于**"查法给假阴性 / 单样本以偏概全"**：grep `include` 过滤、丹药与装备两套品质词表、把 JSON 字符串当对象取属性、拿一只怪推断全局。规则：断言"不存在"前必须换一种查法交叉验证；断言"普遍"前必须全量统计。
- 探针必须改**真正被读取的那个字段**——曾因改怪物模板顶层 `speed`（实现读的是 `stats` 内的值）得出无效结论。
- 破坏性演练 + 备份哈希核对，是本轮唯一能证明"恢复可用"的手段；不演练的备份不算备份。
