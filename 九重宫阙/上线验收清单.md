# 九重宫阙 · 上线验收清单（**由 `node scripts/gen-acceptance.js` 生成**）

> 本文件是**产物**，勿手改：每项状态由实测推导（存档副本只读探针 + sim 的 DSH-HEADLINE 机器块 + 子进程实跑 + 源码 grep 判接线）。
> 改数值后跑 `npm run accept:gen` 重出；门禁第 21 套 `P5 验收清单新鲜度` 会逐字比对，文档一过期就红。
> 取不到的数据一律写「未取到」，不拿历史叙述当证据，也不用估算填格子。

## 结论：**NO-GO**

仍有 4 项门控未过：「docker build + 真守护进程 up + 容器内 /api/health」、「备份→恢复演练在容器里再跑一遍」、「30 分钟第三方独立部署演练」、「前端覆盖率清单人工核对 20 条」。

**环境/人工门（4 项）**：docker build + 真守护进程 up + 容器内 /api/health；备份→恢复演练在容器里再跑一遍；30 分钟第三方独立部署演练；前端覆盖率清单人工核对 20 条 —— 机器判不了或本机条件不具备，按章程必须由人签核，**不用自评把它们划掉**。

## P5 上线门控七项

| # | 门控项 | 判定 | 依据（实测） |
|---|---|---|---|
| 1 | npm test 全绿且断言覆盖本轮全部机制 | 🟢 过 | 上一次完整门禁：21 套，失败 0（本工件描述的是**上一次**门禁 —— 新鲜度套件跑在计数写出之前，滞后一格是结构性的，不假装实时） |
| 2 | 六条不变量逐条有断言（含第 3 条全表引用扫描） | 🟢 过 | ref-integrity：通过；🟢 全部引用边闭合（0 悬空） |
| 3 | docker build + 真守护进程 up + 容器内 /api/health | 🔴 不过 | 需本机 docker 守护进程（历史轮次记录为"仅 CI 侧证据"，本机从未验证成功 ⇒ 判红不放水） |
| 4 | 备份→恢复演练在容器里再跑一遍 | 🔴 不过 | 依赖上一项的容器环境；宿主机侧演练早已通过（E7） |
| 5 | 30 分钟第三方独立部署演练 | 🔴 不过 | 需要一个人以外的人来做，机器判不了 ⇒ 保持阻塞，不用自评糊过去 |
| 6 | 数值复跑四张表贴进本清单 | 🟢 过 | 追赶 8/失败0　经济 11/失败0　R11 通过（sim-breakthrough 3000 世；其内部硬断言含"渡劫寿尽率 <5%"，退出码即结论，不再靠解析它的措辞）　战斗 通过（sim-battle 四段胜率带；退出码即结论） |
| 7 | 前端覆盖率清单人工核对 20 条 | 🔴 不过 | 机器侧已有：后端 276 端点 / 可点 167 / 幽灵 0；人工 20 条未做 |

## 门禁证据

- 上一次完整 `npm test`：**21 套**，失败 **0**（清单新鲜度套件在本工件写出之前跑，故这里滞后一次门禁，属结构性滞后，如实标注）。
- 本轮实测：追赶校验 8 通过 / 0 失败；经济守恒 11 通过 / 0 失败；R11 渡劫 未取到（寿尽 ?、卡死 ?，线 <5%）；战斗 未取到；全表引用 通过。
- 数值表：`数值追赶校验.md`、`经济守恒表.md`、`零实例表可达性审计.md`、`前端可见性与覆盖率测量.md`（前两张尾部带 DSH-HEADLINE 机器块，本清单只引用机器块，不引用正文）。

## E1→E10 逐项状态（由实测推导）

| 项 | 状态 | 已交付且有证据 | 缺口 |
|---|---|---|---|
| **E1 写安全** | ✅ | 并发守恒 7 条在第 18 套内（50 路同购余额精确到枚 / 库存只够 K 次成功恰 K / 20 路锻造双守恒 / 混合写四项同精 / 多角色无重复 id）；auth 层按角色串行 + 15s 看门狗 | 脏集合增量写（性能欠债，单实例下正确）；getNextId 在"await 后 push"交错下无测试 |
| **E2 安全加固** | 🟡 | 攻击模拟 11 条在门禁内；坏 token 五种全 401、爆破 423、注入不绕认证、XSS 道号净化、越权 admin 被拒、超大 body/畸形 JSON 回 JSON 不回堆栈；trust proxy 改显式开关 | 未做：helmet/CSP 未引入（现只有自备头）；schema 级入参校验未做（现只有 sanitize）；DSH_TRUST_PROXY=1 无真实反代验证；登录锁定为进程内状态 |
| **E3 战斗接线** | 🟡 | 槽位 min(2+境界,8) 且满槽拒、比值减伤 def/(def+K)、掉落真入包 + 5 次保底按敌级分档、先手按 speed、怪物模板入战（修 id 被当 mapId）；sim-battle  在门禁内 | 技能 MP/冷却/effect_type 未被战斗结算消费（grep 判不到消费点）；player_skills 实例 10 行 vs 定义 0 条 ⇒ 加得越多空转越多；怪物 drops 透传未消费 |
| **E4 修炼结算** | ✅ | 九乘区模型 已接入生产路径（services/cultivation.js 引 cultivation-model，computeCultivation 给总乘区）；闭关/环境/灵植/资质/燃寿各区有 pending 上报；曲线真源 = realms.exp_requirement，十境最大偏差 0% | 突破立涨 ΔL 未在面板显示；db.gongfa 实例 0 行 ⇒ 功法乘区对线上玩家实际仍为 1.0（可达性缺口，非空通道） |
| **E5 大限劫/延寿/丹毒/燃寿** | ✅ | 大限劫：age≥cap 不再静默死亡，改 pending 窗口 + Boss 应劫（第 9 套 E5 路由 6+ 条锁）；延寿：三件灵植买得到/用得动/看得见三段硬锁（第 20 套），经济侧 35% 闸唯一写入器；契机丹全链路实测金丹 70→85 | 延寿通道尚有 3 条挂账（延寿丹四档 items 里带 longevity_ratio 的 = 0 件／宗门赏赐无兑换口／长生功无年度消费）；丹毒四档与燃寿端点按 R5/R6 决策未列入上线阻塞 |
| **E6 数据深化** | ✅ | 行数实测（存档副本）：材料 86（目标 86）· 地图 32（32）· 图纸 40（40）· 副本 50（50）· 技能定义 0（320）· 功法定义 132（130）· items 总 633；台账往返 verify:rebuild 🟢 | 技能定义 0 vs 目标 320 的差额与 67 条 legacy skills 同源（P1 已按差口补足并锁行数下限）；items 重名 23 组 / maps 妖兽森林 ×2 未清 |
| **E7 交付物** | 🟡 | Dockerfile + compose（name 显式，config -q 通过）、PM2 fork/instances=1、备份→校验→破坏性恢复演练（删表→0 行→还原）、CI（测试+audit+容器冒烟轮询 health） | 本机无 docker 守护进程 ⇒ 容器内恢复与真 up 仍只有 CI 侧证据（P5 第 3/4 项判红） |
| **E8 好友与市场经济** | 🟡 | friends 集合 0 行、五端点在角色锁内、上限 50、只认 accepted；市场 7 日成交均价/中位/指导价 + ±40% 拒单 + 1h 3 单 + 税 3% 逐分守恒（第 11 套 10 条）；市场价来自真实成交 | 赠礼守恒、亲密度 ≥50 组队 / ≥150 借技（蓝图 §6.2 宽口径）、前端好友面板未做；db.sect_members 0 行 ⇒ 宗门赏赐类通道不可达 |
| **E9 数值验证** | ✅ | 追赶校验 8 条全绿：裸修余量带 炼气[60,80] 筑基[40,55] 金丹[25,35] 元婴[20,28]（双边硬锁，判定口径 P1 裸修）；经济守恒 11 条全绿：印钞机 0／289 条在售货架、品质词未覆盖 0 件、死键 0、白送无界 0、采集水龙头越 5% 的 0 张；修为量级：单次 16000=0.308%、合计 1.86%（线 0.5%/2%）；量尺画像功法取自 items 真源（玄冰护体神功 ×1.12 / 永恒不灭 ×10）；R11：渡劫寿尽 ?%（线 <5%）、卡死 ?% | 挂机收益倒挂 2 对未修（非阻塞，已登记）；mods 阵法/灵脉取值未实测 |
| **E10 前端闭环** | 🟡 | 静态：后端 276 端点 / 玩家可点 167 / 幽灵 0（第 21 套之外由 coverage:api 出表）；动态：S3 把 31 个页签在无浏览器环境下真跑一遍（不抛异常、非空白、无 undefined 泄漏、onclick 有定义、进度条分母>0），当场修掉五处真缺陷；突破来源/槽位 n÷8/保底进度/423+429 语义/背包延寿按钮均已可见 | **未做真浏览器（Playwright 级）交互与视觉验证** —— "注册→修炼→突破→应劫→转世"一次纯点 UI 闭环至今没有实测记录；蛇形/驼峰契约未归一；public/js/character.js 与 game.js 两个孤档待删 |

## 六条不变量符合性

1. 寿元随境界单调 + `bonus ≤ cap×35%` → ✅（十四期断言；延寿唯一写入器 `gameTime.addLifespanBonus`，第 20 套锁"无第二处寿元算术"）。
2. 寿元计算全按比例制 → ✅（`yearsOfRatio`，100→1,000,000 跨度已验证）。
3. 内容 = 定义 + 幂等 seed + 断言，幽灵数据 0 → ✅（ref-integrity 全表引用扫描本轮实跑：通过；覆盖率表幽灵 0）。
4. 改角色的端点须在角色锁内 → ✅（auth 层统一持锁 + 无锁版并发判红的反证）。
5. 新常量只进 `balance.js` → ✅（本轮品质表/系数进 `routes/shop.js` 并由 sim 从**源码抽取**复用，量尺与实现不各存一份）。
6. `npm test` 全绿方可提交 → ✅（全程遵守；第 21 套把"文档新鲜"也纳入门禁）。

## 若要上线，最小必要集（按代价升序，实测推导）

4. **E3 技能真源接线**：`player_skills` 实例 10 行 vs 定义 0 条；MP/冷却/effect_type 若无人消费，加内容等于加空转。
5. **E10 真浏览器冒烟**：一次"注册→修炼→突破→应劫→转世"纯点 UI 闭环（含"学一个功法后功法乘区从 1.0 抬起"，正好收 E4 的 gongfa 实例缺口）。
6. **E2 输入校验层**：schema 级校验（现只有 sanitize），是最可能出越权/溢出的面。
7. **容器与人工签核**：docker build/up、容器内恢复演练、30 分钟独立部署、覆盖率 20 条人工核对 —— 这四项机器代不了。

## 本会话的流程性收获（写给人看）

- 断言"不存在"前必须换一种查法交叉验证；断言"普遍"前必须全量统计。本清单里所有"未做/判红"都附了判定手段（grep 模式 / 实跑 / 行数），可复核。
- 探针必须改**真正被读取的那个字段**；文档必须引用**真正生成的那个产物**。
- 备份不经过破坏性演练不算备份；清单不经过门禁比对不算清单。

