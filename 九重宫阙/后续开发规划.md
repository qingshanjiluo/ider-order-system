# 九重宫阙 · 后续开发规划（第 31 轮起执行）

> 本规划的唯一依据是**实测缺口**，不是记忆或叙述。第 30 轮验收结论为 **NO-GO**（见 `上线验收清单.md`）。
> 每项都规定了"可机器判定的验收断言 + 实测证据形式"，未取到证据不得写"已完成"。

---

## 〇、事实基线（2026 轮30-31 实测，后续勿凭记忆覆盖）

### 自动化门禁
- `npm test` 退出码 **0**；共 **157 项**：内容 99 / 单元 18+8+10 / E1 锁 7 / E1 auth 锁 5 / E2 限流 5 / E3 掉落 5。
- 套件由 `scripts/run-all-tests.js` 用 `spawnSync(process.execPath, [file], {stdio:'inherit'})` 串行跑，**无超时**、透传真实退出码。

### 数据实量（`loadDatabase()` 直接点数）
| 集合 | 行数 | 对照章程 E6 目标 | 差口 |
|---|---|---|---|
| items（总） | 566 | — | — |
| items 且 `type==='材料'` | **77** | 86 | +9 |
| monsters | 86 | — | 达标 |
| maps | 20 | 32 | +12 |
| dungeons | 32 | 50 | +18 |
| blueprints（图纸） | **21** | 40 | +19 |
| recipes / forge_recipes | 25 / 28 | — | 未列入目标 |
| shop | 136 | — | — |
| realms | 10 | — | 飞升在第 10 行，`LIFESPAN_YEARS['飞升']=null` |
| users / characters | 32 / 32 | — | 多为骨架样本 |
| **player_skills** | **10** | 技能库 214 定义 | 真源接线严重不足 |
| **skills 表** | **0** | — | 技能定义在 `src/services/skill.js` 代码内数组（非 DB 表），空表**不是缺陷**，但须确认无代码依赖 `db.skills` |
| **gongfa 表** | **0** | — | 🔴 **没有任何角色真正获得过功法** → 必须查"学习入口是否可达"，而非只补定义数 |
| **pets 表** | **0** | 灵兽定义已 17→36 | 🔴 同上：灵兽内容做完了但**实例为零** |
| equipments | 3 | — | 偏低 |
| achievements | **1** | 成就定义 50 | 🔴 进度落库几乎不存在，`friends` 类成就永不可达 |
| inventory | 10 | — | 行形状 `{id,character_id,item_id,quantity}` |
| character_buffs | 2 | — | — |
| **friends** | **不存在** | E8 | 🔴 既无集合也无 `col_friends` 字样 |
| market_listings / market_orders / market_rates | 关系表（阶段1 预建） | E8 | 经 `store.queryRel/insertRel` 访问，**不是** `db.market_orders` |

### 前端
- `public/js/`：`api.js` 20KB（`const API_BASE='/api'` + `request(method, path)` 封装，带去重/超时/重试）、`app.js` **159KB / 3510 行**（单体）、`ui.js` 13.6KB、`character.js`/`chat.js`/`game.js` 均 <5KB、`css/style.css` 24.9KB、`index.html` 15KB。
- 前端**不是**无 API 调用：字面量 `/api/` 数为 0 只是因为 base 前缀拼接。**真实覆盖率尚未可靠测出**（我的静态解析脚本在挂载前缀上判坏，得出 0% 是假数，不采信）。

### 已交付且带证据的机制（防重复劳动）
- E1：`src/middleware/auth.js` 按 `characterId` 端到端串行 + 15s 看门狗；`charLock.js` 队列；**反证**：去锁后并发进入=8。
- E2：`trust proxy`、生产 `JWT_SECRET` 断言（弱密钥 `process.exit(1)`）、`loginGuard`（实测 401×5→423）、`tierLimit`（实测 401 仍带 `X-RateLimit-Limit=60`、health 豁免）。
- E3：槽位 `min(2+境界,8)` 满槽拒绝；比值减伤 `def/(def+K)`（`balance.mitigatedDamage`，封顶减 75%）；掉落真入包 + 5 次保底 + 按敌级分档；先手按 `speed`；`rounds` 差一修复；**怪物模板入战**（`buildMonsterFromTemplate`，86 只中 66 只曾因 `id` 当 `mapId` 而无法出场并返回 null 对手）。
- E4：突破概率展示与结算同源（`resolveBreakthroughMods`）；失败按比例折寿 + 心魔 +1 + 不降境界。
- E5 部分：**破境丹**全链路（持有 +15%、判定后一次性消耗、坊市 5000 可买，实测金丹 70→85）。
- E9 部分：`sim-breakthrough` 支持契机丹 0/2/5 枚对照；R11 护栏达标（渡劫寿尽 24.2%→**0.2%**，卡死率恒 0）。
- E7：Dockerfile/compose（`name:` 显式，`config -q` 退出 0）、PM2（fork/1）、备份→校验→**破坏性恢复演练**、CI（测试+audit+容器冒烟轮询 `/api/health`）。

---

## 一、优先级原则（为什么是这个顺序）

1. **玩法承诺优先于内容数量**。"寿元只决定死亡与转世"是这游戏的立身之本，而寿元耗尽现在**只是悄悄死亡**（大限劫零实现）。补 100 条技能也救不了一个没有终局体验的循环。
2. **"实例可达性"优先于"定义数量"**。E6 的 82→130 之类指标，如果 `gongfa`/`pets` 表恒为 0，做了等于没做（不变量 3 禁幽灵数据）。
3. **验收由章程自定标准判**，不由我的感觉判：E1 要 50 并发守恒、E2 要攻击模拟脚本、E3 要 `sim-battle` 四段胜率、E9 要铁律(1)(2)(3)(4)、E10 要"纯 UI 走完关键闭环"。这些**目前全都没有**，是上线判定的真短板。
4. 每轮**只做能在预算内"读完→改完→验证→提交"的一件事**。宁可少做，不做未取证的东西。

---

## 二、P0 · 玩法闭环（最高优先，串行执行）

### T0-1 大限劫：寿元耗尽改为触发劫难（E5 核心，L）
- **先定位**：`grep 'age >= ' / 'passAway' / 'lifespan'` 于 `src/services/**` 与 `src/routes/cultivation.js`，找出寿元耗尽的**唯一判定点**（不得凭空新增第二处，避免双写）。
- **改法**：`age >= cap` 时不再直接陨落 → 置 `character.tribulation = { stage:'pending', expires_at_game_year: age + 3 }`，给 3 游戏年内应劫窗口；进入一场 **Boss 战**（复用 `combat.startBattle`，敌人为 `buildMonsterFromTemplate` 挑选 `level_range` 匹配当前境界的高阶模板，或专用"大限劫"模板）：
  - 胜 → 续寿 `balance.LIFESPAN_YEARS[realm] × 0.10`（走 `yearsOfRatio`，禁绝对值），清 `tribulation`，写编年史 `type:'劫数'`（`chronicle.js` 已有该标签）；
  - 败/超时 → 陨落转世（沿用现有转世路径，若不存在则本任务必须一并落地，否则终局无出口）；
  - 应劫中禁止再触发第二次（幂等：以 `tribulation.stage` 为闸门）。
- **复用**：`realm.isTribulationRealm(realmName)` 已存在，用它决定应劫强度（化神及以上才有"劫"，以下只是"寿尽"）。
- **验收断言**（新增"大限劫期"到 `scripts/test-content.js`）：
  1. `age = cap` 时角色**未死亡**且 `tribulation.stage === 'pending'`；
  2. 连续两次结算不产生两个 pending（幂等）；
  3. 胜利分支寿元增量 = `cap × 0.10`（比例制断言，禁绝对值常量）；
  4. 失败分支走转世且不可回滚；
  5. `大限劫` 关键字在 `src/` 命中数 > 0（反向锁住"零实现"这一状态不得复发）。
- **证据**：临时探针脚本（备份 `game.db` → 造 `age=cap` 的内存角色 → 跑结算 → 断言 → **MD5 还原**），像轮 29 那样做。

### T0-2 九乘区修炼速度模型（E4，M）
- 新建 `src/services/cultivation-model.js`：速度 = 九区连乘（灵植/功法/资质/浓度/属性契合/闭关/丹毒/伤势/洞府），全部系数从 `balance.js` 读（不变量 5）。
- **铁律断言（章程 E4 原文）**：速度**只能填满当前境界的修为，不能绕过突破瓶颈**——即 `exp` 上限钉在 `exp_requirement`，多余速度归零，不得累积到下一境界。
- 接入现有 `routes/cultivation.js` 结算（现在用的是旧公式；先读该路由再改，勿假设）。
- **验收**：`sim` 输出"同角色开启/关闭闭关"两曲线，速度差异落在 `SECLUSION` 档内且不改变突破概率。

### T0-3 E3 剩余接线（L，可拆两轮）
- **技能真源切 `player_skills`**：`combat.getCharacterSkills()` 现在混用 `db.gongfa`（旧体系）与 `player_skills`；以 `player_skills` 为唯一真源，旧体系只保留只读兼容。
- **MP / 冷却 / 状态 / 逃跑 / 合击**：技能定义里**已有** `mana_cost / cooldown / effect_type('dot'|'aoe'|...) / effect_value` 字段（实测 `services/skill.js` 数组），但战斗循环**完全没消费** → 这是纯粹的空转，接上即可，无需新增数据。
- 验收：`sim-battle` 四等级段胜率落 **75-92 / 60-78 / 45-65 / 30-50%**（章程 E3 原文），脚本入 `scripts/sim-battle.js`。

---

## 三、P1 · 数据与真源可达性（先查可达，再补数量）

### T1-1 三张零实例表的可达性审计（S，必须先做）
- `gongfa=0`、`pets=0`、`achievements=1`：逐表回答"玩家有没有合法入口能造出第一行？"
  - 有入口但样本没触发 → 在 seed 或测试里跑一次，留证据；
  - 无入口 → 这是**功能缺失**，优先级立刻升到 P0 同级。
- 产出：`开发日志.md` 一张三行结论表 + 每条附"我点了哪个函数/路由"的证据。

### T1-2 定义数补齐（M～L，按差口从小到大）
| 品类 | 现状 | 目标 | 差口 | 落点 |
|---|---|---|---|---|
| 材料 | 77 | 86 | +9 | `src/services/materials.js` |
| 地图 | 20 | 32 | +12 | `db.maps` seed |
| 图纸 | 21 | 40 | +19 | `db.blueprints` seed |
| 副本 | 32 | 50 | +18 | `src/data` 副本表 |
| 功法 | 82 定义 | 130 | +48 | 功法库数据文件（**依赖 T1-1 结论**） |
| 技能 | 214 定义 | 320 | +106 | `src/services/skill.js` 定义数组（**依赖 T0-3 真源切换**，否则加得越多空转越多） |
- 每条数据必须：① 名称唯一（幂等 seed 以 name 为键）；② 品质词落在**该品类自己的阶梯**内（丹药/装备两套词表，轮 26 已栽过）；③ 有可消费路径（`validate` 0 违规）。
- 验收：`scripts/test-content.js` 每期新增"数量下限 + 词表合法 + 无重复名 + 无幽灵引用"断言。

> **进度（轮44）**：材料 77->**86** ✓、图纸 21->**40** ✓ 两项达标（走 `src/services/materials.js` 目录 + `npm run content:ensure`，与 boot 同工序，二次 changed=0 幂等）。
> 同一轮把 `ref-integrity` 扩成"引用闭合 + 获取路径闭合"十边审计，抓到并修掉两类既有缺陷：7 条采集配置指向不存在的地图名（静默零命中）、16 件材料无任何获取路径（其中 13 件是丹方/图纸/锻造的耗材 = 学得了炼不成的死链）。
> **进度（轮45）**：地图 20->**32** ✓（新建 `src/data/map-library.js` 目录 + ensureAll 挂 ensureMaps，二次 changed=0）、副本 32->**50** ✓（dungeon-library +18，含把轮42 那 5 个孤儿副本按名接回并给实物产出；drift-probe 复核该 5 行已从"真新内容 5/5"变成"同名重复 5/5、真新内容 0"）。
> 同轮登记的既有缺陷：`monsterStatsFor` 线性 hp 式使**合成怪偏软**（difficulty=6 时 hp=5280 vs 飞升池中位 19061，比值 0.28）——已做成"已知偏差登记"锁，P4 改曲线必须回来复核；以及全库挂机收益倒挂 2 对（都在 混沌深渊 75-90:320，被 天界花园/五行圣地 反超），做成棘轮锁 ≤2。
> **进度（轮46）**：功法 82->**130** ✓（+48 门藏经阁/秘境/传承/机遇/仙源典籍），且先按 T1-1 硬约束 2 解决可达性：`ensureGongfaShelves` 挂进 ensureAll，宗门功法架 4->72 行、58 条非宗门典籍物化上架，实测 功法127/127 灵宠13/13 功法书5/5 材料86/86 全有具名获取路径；补上 `POST /gongfa/study`（设计文档写了却从未实现的"功法书->功法"）；新增第 10 个套件 G1（真 HTTP + 临时档，11 项）首次让 `db.gongfa` 出现真实例行。R11 复跑 渡劫 0.1%/卡死 0.0%（具名功法 speed 上限 1.6 vs 随机 1.575，差 0.025，结论不变）。
> **进度（轮47）**：技能 214->**320** ✓ 且全部押在原库为 0 的高境界（化神以上 8 -> 114 门）；效果类型只用战斗状态机真实现的五类，全局"已实现效果占比"66.8% -> **76.6%**；顺手关掉两扇可被任意登录玩家白嫖仙阶大招的门（/unlock-hidden 收客户端自报条件、/learn 不校验 is_hidden）+ 掉落池不卡境界。
> **P4 修为量级上锁（轮57）**：先订正我上轮的误诊 —— 乘区2 的 `cultivation_speed` **不是空通道**（items 98 件带值 1~10，模型 :95 吃它，`cultivation.js:39` 换算），
> 为空的是 `db.gongfa` 实例表 0 行（T1-1 可达性）。量尺画像原先手写功法漏吃这个乘区 ⇒ P2 少算 1.12 倍，已改为从 items 真源取（P1 裸修／P3 撞 12× 总闸 ⇒ 轮54 目标带与铁律(2) 不受影响）。
> 另加**零头锁**：routes 里 78 处 exp 常量，缩放前合计 571,250（整境需求 11.0%）、单笔最大 200,000（成就万人斩 3.85%）⇒ 成就档收敛到 57,100，现值单笔 0.308%／合计 1.86%（线 0.5%／2%）；`sim-balance` 6→8 条，**P4 四项必修全部结案**。
> **P4 回收词表补齐（轮56）**：`QUALITY_PRICE` 缺 238 件（占 38%）品质词 → 0，删 3 个死键；
> 因为实测采集节点含 28 个道品（无取得成本 ⇒ 回收价即纯铸币，而 A 锁只看货架），故只给温和阶梯 3/12/48/192/750，
> 并新增 **A5 采集回收速率锁**（≤该图年挂机收入 5%，实测最紧 1.09%）+ A3 死键锁 + A4 白送行有界锁；`sim-economy` 8→11 条，E9 上线必修只剩 ③。
> **P4 曲线调平完成（轮54）**：`--solve` 反解 → `realm-need.json` 工件 → `apply-realm-need.js` 同步三处定义源，
> 铁律 (1)(2)(3)(4) **首次全部成立**（余量 69.9/48.1/29.7/21.0%，化神 −281%，跳变 ×33 收敛为 ×3.3~4 + ×25 设计墙）。
> 同一轮清出 5 处经验"第二真源"（quests 私有升级循环可绕开突破判定、auth/gameTime 硬编码 100、pet 直写修为池），
> 收敛为 `src/services/exp-curve.js` 单点 + 第 19 套门禁 `G3 经验真源`。R11 强制复跑达标（渡劫寿尽 ≤0.2%）。
> **E9 经济侧同步落地（轮53）**：`sim-economy` 实测 286 条货架**无印钞机**（A 硬锁 0）；
> 但 `QUALITY_PRICE` 缺丹药/材料品质词 ⇒ （轮56 已修：实为 238 件 / 占 38%，见下）**142 件**（旧口径只数丹药+材料）道具回收价一律 3 灵石；`LIFE_GAIN` 延寿四通道**玩家可获得 0/3** ⇒ 铁律(2) 缺执行手段（登记 NO-GO）。**〔轮55 已打通〕**三件灵植上架 + `/shop/use-item` 延寿分支 + 背包「延寿」按钮（三段硬锁），第 20 套 `G4 延寿通道` 9/9；余三档（延寿丹/宗门赏赐/长生功）转 C4 棘轮挂账。
> 本轮更正一处：先前"21 条印钞机"的结论来自手写假品质表，改用 `shop.js` 真函数后为 0 —— 算钱必须用被测代码自己的表。
> **E9 量尺已落地（轮53）**：`sim-balance` 实测铁律 (1)(2)(4) 成立、**(3) 不成立**（每境耗时 ×33 vs 寿元 ×3.3，
> 化神单境常规画像 3.13 万年 > L 2 万年）；由此新增 P4 必修项「**跨境曲线调平**」（牵动寿元经济，改后强制复跑 R11 + E3 胜率基线）。
> **P4 开工（轮52）**：E1 的 HTTP 级并发守恒（50 路购买 / 20 路锻造 / 混合写压 / 反超卖 / 无重复 id）
> 与 E2 的攻击模拟（坏 token 五连、登录爆破、SQL/XSS、越权 admin、超大与畸形 body、双层限流、生产密钥断言）各落地为常驻门禁套件；
> 顺带修掉"畸形 JSON 回 HTML 堆栈页"与"trust proxy 无条件信任代理头"两处真实缺陷。剩余：`sim-balance`（cumT vs L 铁律 1~4）、`sim-economy`（30 世净收支）、schema 校验、helmet/CSP。
> **P3 收尾（轮51）**：`scripts/test-fe-render-smoke.js` 用 vm+DOM 垫片把 31 个玩家页签**真渲染**一遍
> （不抛异常 / 非空白 / 零 undefined 泄漏 / onclick 目标皆有定义 / 进度条分母 > 0 / 无未捕获拒绝），首跑即修掉五处玩家可见缺陷；
> 遗留：后端路由字段蛇形↔驼峰混用的契约归一、两个从不加载的孤档待删、真浏览器交互与视觉验证未做。
> **P3 开工（轮49）**：先立尺子再改界面 —— `scripts/endpoint-coverage.js` 三口径实测（后端 276 端点 / 玩家可点 166=60.1% / 幽灵调用 0），报告由测量器生成；
> 前端传输层已把 423/429/501 语义送回调用方，好友面板接通 `/api/friend` 六端点，隐藏技"必然失败的解锁按钮"已撤。
> **P1（补定义）六项差口全部完成**：材料 86 ✓ 图纸 40 ✓ 地图 32 ✓ 副本 50 ✓ 功法 130 ✓ 技能 320 ✓ —— 下一阶段转 P2（好友集合 + 市场 7 日成交价）。

---

## 四、P2 · 社交与经济（E8，M）

- **好友表新建** ✅（轮48）：集合已注册进 `store.DOC_COLLECTIONS`。字段最终取 **`character_id` + `friend_id`**（不是这里 originally 写的 `target_character_id`）—— 因为 `routes/achievement.js:198` 这个既有消费方读的就是 `friend_id`，按"先接消费方、消费方优先"以代码为准；行内含 `status(pending/accepted/rejected)`、`intimacy`、`created_at/updated_at`。端点扩到五个：申请 / 回应 / 删除 / 列表 / 拜访洞府（+ 搜道号），全部在 `middleware/auth` 的角色锁内；上限 50、只认 accepted、被拒 1 天冷却、拜访日限 3 次。
- `friends` 成就进度必须可 > 0（章程 E8 原文验收）。
- **市场指导价与税率** ✅（轮48）：`GET /api/market/prices` 出 7 日成交均价/中位数/样本数/指导价/允许区间；指导价 = 7 日中位（样本 ≥5）→ 否则坊市基准价 → 否则 `none`（不假装知道价）；偏离 ±40% 的挂单直接拒；同角色同物 1h 最多 3 单（`tierLimit` 只管前缀总量，管不住反复挂撤，故这层单独实现）；沿用 1% 挂单费 + 3% 成交税。**未做（登记）**：赠礼守恒、亲密度 ≥50 组队加成 / ≥150 借技（蓝图 §6.2 宽口径）。
- 验收：集成测试跑一遍 A 申请 B、B 同意、A 看到 B 在列，且 `friends` 成就进度 > 0。

---

## 五、P3 · 前端闭环（E10，L）

1. **先做可靠测量**（我这次没做成的事）：静态解析 `public/js/*.js` 中 `api.get/post/...` 的 path 实参，与 `server.js` 的 `app.use('/api/<name>', xxxRoutes)` + 各 `router.<verb>('<path>')` 做**前缀拼接后**比对，产出"已调用 / 未调用端点"清单。必须打印样例逐条人工核对 20 条再采信（轮 5 的"267/273 未调用"假指标就是没做这步）。
2. 本会话新增机制的**可见性**（不可见等于没交付）：
   - ✅ 突破面板显示 `chance/parts`（含契机丹 +15 的来源）、失败折寿预告（轮50：面板出口 `breakthroughPanel`，折寿预览与结算同一份实现）；
   - 背包里破境丹的"一次性"说明与坊市可购入口；
   - 槽位上限 `n/8` 与"槽满"错误提示；
   - ✅ 掉落保底进度（轮50：`/api/character` 返回 `lootPity{dryStreak,threshold,nextIsGuaranteed}`，阈值取自 balance；角色页进度条）；
   - 423（锁定倒计时）/ 429（限流 Retry-After）的前端语义化提示，别再把它们当"网络错误"。
3. `app.js` 3510 行单体拆分（按面板/战斗/修炼/市场分模块，ESM 或 IIFE 命名空间），**只在功能项做完后做**，避免边拆边改冲突。
4. 验收：一次完整游玩闭环（注册→修炼→突破→应劫→转世）纯点 UI 可走完，无手工调 API。

---

## 六、P4 · 验收自动化补齐（章程里承诺过、但一直没做的硬指标）

| 缺口 | 章程原文 | 要做的事 |
|---|---|---|
| E1 | 50 并发扣灵石余额精确；20 并发锻造材料不负、成品守恒 | `scripts/concurrency-test.js`：**HTTP 级**（真起服务 + 真 token + 50 并发），不是中间件级 |
| E2 | 攻击模拟脚本通过；无 secret 时 production 拒绝启动 | 后半已做；补 `scripts/attack-sim.js`（越权、路径穿越、超大 body、SQLi 字面量、限流绕过尝试） |
| E3 | `sim-battle` 四段胜率 | 见 T0-3 |
| E9 | `sim-balance`（cumT vs L 曲线）+ `sim-economy`（30 世净收支），铁律(1)(2)(3)(4)通过 | 两个新脚本 + 断言化铁律 |
| E5 护栏 | 契机齐备后复跑，渡劫寿尽 < 5% | 已达标（0.2%），**T0-1 做完后必须再复跑一次**（大限劫改了寿元经济，旧结论不能自动继承） |

---

## 七、P5 · 上线门控（发布前一次性完成）

- [ ] `npm test` 全绿且**新增断言覆盖本轮全部机制**（当前 157 项 → 目标 ≥ 200）
- [ ] 六条不变量逐条有断言（第 3 条"幽灵数据 0"需一次全表扫描：任何被引用而不存在的 name/id 判红）
- [ ] `docker build` + 真守护进程 `up` 一次，容器内跑 `/api/health`（目前只有 CI 侧证据，本机无守护进程）
- [ ] 备份→恢复演练**在容器里**再跑一遍（宿主机已跑过）
- [ ] 30 分钟第三方独立部署演练（章程 E7 验收）
- [ ] 数值复跑：`sim-breakthrough`、`sim-battle`、`sim-balance`、`sim-economy` 四张表贴进 `上线验收清单.md`
- [ ] 前端覆盖率清单（P3-1）人工核对 20 条

---

> **P5 门控已机读化（轮58）**：《上线验收清单.md》改为 `scripts/gen-acceptance.js` 的产物（读 sim 的 DSH-HEADLINE 机器块 + 存档只读探针 + 子进程实跑 + grep 判接线），
> 门禁第 21 套 `P5 验收清单新鲜度` 逐字比对并钉住本轮抓到的五条过期说法；两张数值 sim 在门禁内改带 --report（表不再可能过期），`.test-totals.json` 记录套件数与失败数（滞后一格已在文档里明说）。
> 七项门控现状：机器可解 3 项全绿（npm test 21 套 / 不变量含 ref-integrity 全表扫描 / 数值四表内嵌），**剩 4 项环境人工门（docker、容器内恢复、30 分钟独立部署、20 条人工核对）判红不放水** ⇒ 结论仍是 NO-GO，但是"说得清为什么"的 NO-GO。

> **清单的缺口列改探针推导（轮59）**：E3「MP/冷却未接线」与 E4「db.gongfa 不可达」经实读代码证明是过期手写散文（skillState 的 canUse/spend/applyEffect/tick 全在消费；gongfa.push 路径由 G1 端到端锁）；
> E6「技能定义 0」是读错集合，改为运行时实测 320 条。删掉谎称成功的空转端点 /skills/learn，端点基线显式 276→275（附理由，不松阈值）。S4 覆盖率接进门禁。
> 流程规则：改完必须"跑一遍 npm test → 重出清单"才收敛（清单嵌的是上一次门禁的计数，固定点需一次迭代）。

> **grep 只算线索不算证据（轮60）**：上一轮凭"有消费点"判 E4 可达，实为不可达 —— 坊市路径把 API 键 cultivation/combat 落库，读方过滤中文词，
> 于是买装功法在九乘区与战斗里都不生效（占槽位、零作用）。修法为"写库处 canonType 归一、读方不动"，并立下规则：**凡"接线/可达"类结论必须有把数据真喂进去的端到端断言**。
> 同时新增 E11「谎报成功的端点」扫描（success:true 而无任何写入/委托标记，基线 0 只降不升）与功法 type 归一静态锁。

> **轮61：WAL 裸拷贝丢档（容器恢复演练抓出）**：store.js 开 WAL，备份若 copyFileSync 主库会丢掉未 checkpoint 的已提交事务；旧自检"副本 vs 源逐字节"是恒真式。
> 已改为 VACUUM INTO 一致快照 + 语义核对（integrity ok + 集合行数），并新增第 23 套 7 条锁与 npm run drill:docker（15 步真演练，产物《部署演练.md》）。
> P5 现状：**只剩第 7 项人工核对**；待办：修《覆盖率待核清单》生成器的端点正则（报告里只有路径没有方法），让门控去数人勾的框。

> **轮62：P5 只剩第 7 项，且性质上是"人"**：《覆盖率待核清单.md》20 条带前端证据，勾必须由人打；
> 第 23 套第 ⑤ 条锁住"清单声称的已签数 == 表格里真有的勾，未签满不许判绿"。生成器 review:gen 重生成只保留旧勾、绝不新增。
> 方法论补丁：改完生成器必须**直接跑一次看退出码与完整尾部**（node --check 抓不到 ReferenceError）；不要用 Select-String 过滤跑测输出（曾把报错过滤成盲区）。

> **轮63：E2 的校验器一接线，当场打穿 5 个套件的注册路径**。`VALIDATION_RULES` 此前全仓零引用（写了没挂＝死代码）；挂上 `/login`+`/register` 后立刻暴露两件事：
> ① 5 个套件的测试具名 `tag_13位毫秒_4位随机` 长 21–26 字符，**超产品自己的 username 3–20 规则** ⇒ 一律 400（以前从没暴露，因为 register 压根不校验）；
> ② 其中 `concurrency-test` 与 `attack-sim` 的异常路径**不强制退出**（只设 `process.exitCode`、server 句柄未关、`http.request` 无超时），把整轮门禁**静默吊死**——实测一次 38 分钟、一次 8 分钟以上，`spawnSync` 的 timeout 挡不住未 settle 的 await。
> 修法：具名压进规则；`test-social-market-e2e / test-gongfa-e2e / test-p3-visibility-e2e` 补 `setTimeout(() => process.exit(process.exitCode), 300).unref()`（沿用仓内 `test-fe-render-smoke.js` 的既有写法）；并发与攻击两套加**看门狗**（240s/300s 未退出 ⇒ 先打印判红再强杀，把静默假死变成会说话的红——当场生效过一次）；`http.request` 补 20s 超时与 `timeout` 事件。
> 顺带修掉一个真 bug：`sanitizeInput` 只删 `<>"'&`，于是 `<script>alert(1)</script>道号` 留下 23 字符的**标签尸块**——既不算"洗干净"，又撞上 nickname 的 20 字符上限，表现为"带 XSS 前缀的合法汉字道号被误杀"。改为先整段去标签再删裸字符（`<b>青衫客</b>` → `青衫客`，`<img src=x onerror=alert(1)>甲乙丙` → `甲乙丙`）。
> E2 已落地：`src/middleware/secureHeaders.js`（无 helmet，手写 9 个头 + 去 `X-Powered-By` + 条件 HSTS）、`validate.js` 重复规则键收敛（**146 字面量 / 146 生效**，原先第二个 `type` 静默覆盖前者）、login/register 挂 `validateRequest`、新增第 24 套 9 条真 HTTP 锁；清单 E2 行改探针推导。
> **残留（如实，不伪装成已完成）**：CSP 仍含 `'unsafe-inline'`（前端 14 处内联事件未清）；`DSH_TRUST_PROXY=1` 未经真实反代验证；登录锁定仍是进程内状态（多实例不共享）；`sanitizeInput` 对**密码**同样剥 `['"<>]&`，不同真密码可能塌缩成同一 hash —— 已知未修。

> **战斗技能链的四处真断链（轮63 只读审计结论，尚未修）**。先更正我自己记下的话：我之前写"`player_skills.equipped_slot` 是死通道"，**这个表述是错的**——它有活写者（`services/skill.js:301 equipSkill` ← `POST /api/skill/equip` ← `public/js/app.js:1870`）也有活读者（`combat.js:356`），副本实测装一个 main 技后战斗池 `0→1`。真断点是另外四处：
> ① `GET /api/battle/skills`（`routes/battle.js:81`）返回 `db.gongfa`（**实测 0 行**），而战斗读 `player_skills` ⇒ 界面「选择技能」永不渲染、`app.js:535 currentBattleSkill` 恒 0（只能用池里第一个主技）；
> ② `routes/skill.js:20/142/151` 数的是 `s.equipped`（该键不存在，真键是 `equipped_slot`）⇒ 境界总闸 `min(2+i,8)` **恒不触发**（实测炼气号 cap=2 连装 6 件全 200），且 `cdPenalty` 恒等于 1 却在前端当「冷却×N」展示给玩家；
> ③ `equipSkill` 不校验 `skillDef.slot === slot` 也不拒 `type==='passive'` ⇒ sub 技可进 main、被动技可装备但被 `combat.js:360` 静默丢弃（占槽零收益）；UI 只有「主/副」，19 门 `ultimate` 无入口；
> ④ `routes/forge.js:437-444` 器灵技 push 的行**没有 `equipped_slot` 键**且 `skill_id='spirit_<itemId>'` 不在 SKILLS_DATA ⇒ 永久孤儿行（当前 0 行，属潜伏）。
> 另两处空转：`services/battle/skill.js`（编号槽 1..cap）在 `src/` 内**无任何 require**＝死代码；`combat.js:299 skillDamageMultiplier`（战斗功法乘区）全仓仅此一处赋值、`damage.js` 从不消费（只能"补消费"或"删字段"，不许留着骗审计）。
> **槽位共四套口径**：A 功法 3/6（`routes/gongfa.js:7-10`，对战斗零影响）／B 编号槽（死代码）／**C `services/skill.js:10 SLOT_LIMITS={main:3,sub:3,ultimate:1}`＝战斗唯一有效闸**／D 境界总闸（因读错字段空转）。
> 修法性价比序：①（选招与战斗池同源，前端不必改）→③（6 行校验）→②+④（**注意 ② 一修总闸立即生效＝玩家可感知收紧，须同轮改前端展示并删掉 `cdPenalty`**）→⑤器灵行→⑥乘区→⑦新手"学＋装"或删「起手」措辞（仓里没有建号发放技能的代码，`BASE_SKILLS` 只打折）。

## 八、风险登记（已知，别踩）

1. **测量法造成的假阴性**（本会话已犯 5 次）：`grep include` 过滤、两套品质词表、把 JSON 字符串当对象取属性、单样本以偏概全、静态解析未拼前缀。**规则**：断言"不存在"前换一种查法交叉验证；断言"普遍"前全量统计；探针必须改**真正被读取的那个字段**。
2. **`node -e` 的双引号会被传输层剥掉** → JS 内一律用反引号字符串，或把逻辑写成临时脚本（跑完即删）。
3. **门禁命令不得接 `Select-Object -First N`**（会掐断子进程、`$LASTEXITCODE` 失真）→ 先 `> 文件`，取退出码，再看内容。
4. **PowerShell 字符串里塞裸双引号**（`Add-Content -Value`）反复炸；已改用单引号 here-string `@' … '@`。
5. **`Start-Process -RedirectStandard*` 在本机触发 `NO_PROXY` 字典冲突**；`ForEach-Object -Parallel` 触发沙箱 `spawn EPERM` → 并发测试要换写法（进程内 Node `http` 并发即可，轮 19 已验证可行）。
6. **超时残留进程**：一次 120s 超时后确实可能留下占着 `data/game.db` 的 node；复跑前先 `Get-CimInstance Win32_Process -Filter "Name='node.exe'"` 按**监听端口**判身份（3080 是 DSH 宿主，绝不可杀）。
7. 工具预算长期透支：**不要在无法整读的文件上动手**，这是 30 轮里唯一稳定的零返工来源。
8. **长任务不得走前台单调用**（上限 600s，24 套门禁已超线）⇒ 一律 `run_in_background` + 轮询有界等待。被切断的门禁不会执行它的字节还原，主库会留下 WAL checkpoint 后的形态（轮63 实测 MD5 变了但逐集合语义一致）；恢复流程＝副本探针语义比对 → `git checkout -- data/game.db` → 复验 MD5。
9. **"永不结束的测试"比"会失败的测试"更危险**：套件在异常路径上不强制退出（只设 `exitCode`、句柄未关、请求无超时）会让整轮门禁静默假死，`spawnSync` 的 timeout 也挡不住。凡起 server 的套件必须有**看门狗或 300ms 强退**；新增同类套件时照抄 `test-fe-render-smoke.js` 的退出写法。
10. **看结论要看原文全量**：`Select-String` 只显示命中行（丢续行）、自写打印器 `split('\n')[0]`（吞名单）、`--check=0`（可能检的是没落盘的旧文件）——轮63 一次内犯了这三样，每次都让"证据"看起来比实际少。

## 九、明确不做（防范围蔓延）

- 不做 PG 迁移（触发线：DAU>300 / 写均值>80ms / p95>250ms，均未触及）——现状同步全量写无交错风险。
- 不补"脏集合增量写"作为功能项（E1 的架构性欠债，性能优化，非上线阻塞；单实例下当前实现正确）。
- 不售卖/产出 传承点、道行（R8 决定）。
- 不做境界降阶惩罚（R6 决定）；不改时钟 `24h=10y` 与 D5 寿元阶梯（R5 决定）。
- 不在 P3-1 之前拆 `app.js`。
