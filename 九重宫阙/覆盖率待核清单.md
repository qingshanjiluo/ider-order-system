# 前端覆盖率待核清单（**由 `node scripts/gen-review-sheet.js` 生成**）

> P5 第 7 项要求「前端覆盖率清单人工核对 20 条」。**最后一列的勾必须由人打**：
> 打满 20 行之前，《上线验收清单.md》第 7 项保持判红 —— 生成脚本只保留已有的勾，绝不新增。
> 输入是《前端可见性与覆盖率测量.md》（门禁第 21 套每次带 --report 重出，不会引用过期数）；
> 只收在路由源码里真出现过的路径，文档示例占位不算端点。
> 核对方法：
> - 「前端有调用」：照证据那行去界面点一次，确认请求真发出、响应真渲染（不是只有 fetch 没有回显）。
> - 「前端未见调用」：判断它是有意不做（写进设计）还是漏做（记进《后续开发规划》），二者都算核对完成。

| # | 类别 | 端点 | 前端证据 | 人核对（打勾即签字） |
| --- | --- | --- | --- | --- |
| 1 | 前端有调用 | `/api/achievement/claim` | api.js:616 return this.request('POST', '/achievement/claim', { achievem | - [ ] |
| 2 | 前端有调用 | `/api/achievement/progress` | api.js:612 return this.request('GET', '/achievement/progress'); | - [ ] |
| 3 | 前端有调用 | `/api/admin/broadcast` | api.js:939 return this.request('POST', '/admin/broadcast', { content }) | - [ ] |
| 4 | 前端有调用 | `/api/admin/characters` | api.js:930 return this.request('GET', `/admin/characters?page=${page || | - [ ] |
| 5 | 前端有调用 | `/api/admin/chat-logs` | api.js:933 return this.request('GET', `/admin/chat-logs?limit=${limit | | - [ ] |
| 6 | 前端有调用 | `/api/admin/chat-reports` | api.js:936 return this.request('GET', `/admin/chat-reports?status=${sta | - [ ] |
| 7 | 前端有调用 | `/api/admin/dashboard` | api.js:894 return this.request('GET', '/admin/dashboard'); | - [ ] |
| 8 | 前端有调用 | `/api/admin/items` | api.js:921 return this.request('GET', `/admin/items?search=${search ||  | - [ ] |
| 9 | 前端有调用 | `/api/admin/recharge-logs` | api.js:924 return this.request('GET', '/admin/recharge-logs'); | - [ ] |
| 10 | 前端有调用 | `/api/admin/reset-character` | api.js:927 return this.request('POST', '/admin/reset-character', { user | - [ ] |
| 11 | 前端未见调用 | `/api/achievement` | （前端未见调用） | - [ ] |
| 12 | 前端未见调用 | `/api/admin/users/*/add-item` | （前端未见调用） | - [ ] |
| 13 | 前端未见调用 | `/api/admin/users/*/add-jade` | （前端未见调用） | - [ ] |
| 14 | 前端未见调用 | `/api/admin/users/*/add-stones` | （前端未见调用） | - [ ] |
| 15 | 前端未见调用 | `/api/admin/users/*/ban` | （前端未见调用） | - [ ] |
| 16 | 前端未见调用 | `/api/admin/users/*/detail` | （前端未见调用） | - [ ] |
| 17 | 前端未见调用 | `/api/admin/users/*/role` | （前端未见调用） | - [ ] |
| 18 | 前端未见调用 | `/api/admin/users/*/set-level` | （前端未见调用） | - [ ] |
| 19 | 前端未见调用 | `/api/ads` | （前端未见调用） | - [ ] |
| 20 | 前端未见调用 | `/api/ads/*` | （前端未见调用） | - [ ] |

## DSH-HEADLINE

```json
{
 "kind": "review-sheet",
 "total": 20,
 "wired": 10,
 "unwired": 10,
 "signedNeeded": 20,
 "endpointsSeenInReport": 270
}
```
