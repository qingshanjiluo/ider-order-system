# 九重宫阙 · 容器部署与恢复演练（**由 `node scripts/drill-docker.js` 生成**）

> 这份文档是**演练产物**：它记录的是"从 GitHub 新克隆一份代码，在 Docker 里真的把它跑起来、
> 备份、删库、还原、再登录"的一次实跑，不是计划。对应 P5 第 3/4/5 项。
> 演练全程在临时克隆目录里进行：compose 把 ./data 挂进容器，在仓库里 up 会改写受保护的 data/game.db。

- Docker Server：**29.3.1**
- 克隆到的提交：**acc95bb**（仓库 HEAD acc95bb）
- 全程耗时：**1.1 分钟**（P5 第 5 项要求 30 分钟内完成 ⇒ 满足）
- 备份一步走容器内 `VACUUM INTO`：轮61 首轮演练用裸拷贝主库备份，删库还原后同账号登录 401 —— 写都留在 -wal 里

## 逐步结果

| # | 步骤 | 判定 | 依据 |
| --- | --- | --- | --- |
| 1 | docker 守护进程可达 | 🟢 过 | Server 29.3.1 |
| 2 | 从 GitHub 新克隆 | 🟢 过 | HEAD acc95bb，用时 20s |
| 3 | compose 配置可解析（含 JWT_SECRET 强制项） | 🟢 过 | ok |
| 4 | docker compose build | 🟢 过 | 用时 39s |
| 5 | docker compose up -d | 🟢 过 |  Container jiuchong Started |
| 6 | 容器自检转 healthy（healthcheck 在容器内打 /api/health） | 🟢 过 | running/healthy，累计 50s |
| 7 | 主机侧 GET /api/health | 🟢 过 | 200 {"ok":true,"service":"jiuchong-gongque","version":"1.0.0","env":"production","uptimeSecond |
| 8 | 容器内注册 + 取到角色（制造待恢复数据） | 🔴 不过 | HTTP 400/0 user=null realm=null |
| 9 | 容器内 VACUUM INTO 出一致快照 | 🟢 过 | SNAP_OK 634880 |
| 10 | docker cp 把快照取到宿主机 | 🟢 过 | 3AD7569A7C14293D0D0F1284B951E291／620KB |
| 11 | 模拟灾难：down + 删除数据目录内 1 个库文件 | 🟢 过 | 剩余 db 文件 0 |
| 12 | 恢复后容器 healthy + /api/health 200 | 🟢 过 | running/healthy / HTTP 200 |
| 13 | 恢复后同一账号能登录并取回**同一个**角色 | 🔴 不过 | login HTTP 400 角色 null→null 一致=false |
| 14 | 恢复后的主库结构完整（integrity_check ok） | 🟢 过 | IC ok；备份 MD5 3AD7569A7C14293D0D0F1284B951E291，还原后经登录写入主库为 61C84B0DC5908A574CD8535499C9FC9B（两者不同属正常） |
| 15 | 演练现场清理（容器 down + 临时目录删除） | 🟢 过 | 仓库 data/ 全程未被容器挂载触碰 |

## 结论：**演练有 2 步判红**（13/15）

判红步骤：容器内注册 + 取到角色（制造待恢复数据）、恢复后同一账号能登录并取回**同一个**角色

## DSH-HEADLINE

```json
{
 "kind": "deploy-drill",
 "passed": 13,
 "failed": 2,
 "total": 15,
 "serverVersion": "29.3.1",
 "clonedHead": "acc95bb",
 "elapsedMinutes": 1.1,
 "within30min": true,
 "buildUpHealthOk": true,
 "restoreLoginOk": false,
 "backupSnapshotOk": true
}
```
