# 艾德尔修仙传工作区 · 审阅与整理报告

> **日期**：2026-09-11
> **范围**：全工作区四子项目（原版 / 私服 / 工单系统 / 九重宫阙），九重宫阙为重点
> **方法**：逐文件代码审阅 + 运行时实测（服务器启动 / API 冒烟 / bug 复现）+ git 状态取证 + 数据库完整性扫描
> **结论有效性**：所有标记 ✅ 的条目均已独立验证，非二手转述

---

# 第一部分 · 工作区整理记录

## 1.1 最终结构

```
艾德尔机器人/
├── 原版/          # 原版游戏（Node+Express+MySQL/SQLite）与批量自动化工具链   —— 288 文件
│   ├── 源代码/            # server(含12万行battleEngine) / web-client / crawler / ai_trainer / browser-plugin / data
│   ├── 批量注册工具/       # 28个批量注册/邮箱绑定/自动刷怪/炼丹脚本
│   ├── gh-actions/        # ⚠️ 5文件孤儿目录（见3.1）
│   ├── docs/ sql/ migrations/
│   └── 仙盟仓库管理插件.js / 账号切换*.js / eng.traineddata / 新建文件夹(6)(1).rar
├── 私服/          # 原「私服艾德尔修仙传」，Cloudflare全托管（Pages+Workers+D1+DO）—— 168 文件
│   ├── frontend/ game-api/ chat/ shared/ scripts/ .github/
│   └── PLAN.md            # 私服部署与改造规划
├── 工单系统/       # 代练工单+账号自动化平台（Cloudflare Pages+Functions+D1）—— 1009 文件
│   ├── （原工单系统主体：pages-frontend / functions / worker / gh-actions / 16个迁移SQL）
│   ├── order-worker/      # 独立订单Worker（cron每30分钟，无存储绑定）
│   ├── ider-order-system-clean/  # 2026-08-27历史快照（独立git仓库，含独有withdrawals DDL）
│   └── test-api.mjs / 全面审计报告.md / 皮肤系统完整设计文档.md / test-report-*.md / trial_test_report.*
├── 九重宫阙/       # ★ 当前开发重点：水墨修仙挂机页游（未移动，保持原位）—— 150 文件
├── .github/       # CI工作流（路径引用已全部同步，见1.2）
├── .wrangler/ node_modules/ package-lock.json
└── README.md      # 已重写，记录新结构
```

## 1.2 移动方式与安全性

| 操作 | 明细 |
|---|---|
| git mv 重命名 | **293 个**已跟踪文件/目录（历史保留，处于暂存区） |
| git rm | 3 个根目录副本（deploy_pages.js / run_migration_v8.js / trial_test_report.json，均与工单系统内副本**逐字节相同**，MD5 比对确认） |
| 普通移动 | 私服（重命名）、order-worker、ider-order-system-clean、eng.traineddata（均未跟踪） |
| 垃圾清理 | 1 个 0 字节文件 `h.textContent.includes('Default`（已删除） |
| 工作流同步 | **14 个工作流引用更新**：deploy-ideer.yml 4 处 `私服艾德尔修仙传/`→`私服/`；9 个游戏工作流 `批量注册工具`→`原版/批量注册工具`；4 个根 `gh-actions/`→`原版/gh-actions/`。残留引用检查为零 |
| 不受影响 | 工单系统工作流（`working-directory: 工单系统` 内相对路径）、嵌套 git 仓库（已验证完好） |

**git 状态**：345 项变更已暂存但**未提交**（293 R / 3 D / 38 M / 8 ??）。确认后可提交。

**整理后验证**：九重宫阙服务器正常启动（HTTP 200）；四文件夹文件数 288/168/1009/150。

---

# 第二部分 · 九重宫阙深度审阅（开发重点）

## 2.1 项目概况

水墨修仙挂机页游。Node.js + Express + **JSON 文件数据库**（data/game.json，约 17 万字符）+ 原生 JS 无框架前端 + WebSocket 聊天。**33 个路由模块、231 个端点**，覆盖设计方案全部 14 大系统。

| 层 | 构成 |
|---|---|
| 后端入口 | server.js（226 行：Express + WS + SPA fallback） |
| 路由 | src/routes/ 33 文件（guild 21 端点、battle 12、systems 13、skill 13、alchemy 9…） |
| 服务 | src/services/（character/cultivation/realm/skill/buff/item + battle/combat·damage·skill） |
| 中间件 | auth(JWT) / admin / rateLimit(100/min/IP) / validate(输入消毒) / turnstile / audit |
| 数据 | data/game.json（23 个集合；311 items / 36 monsters / 20 maps / 15 dungeons…） |
| 前端 | public/（api.js 742 行 176 方法、app.js 3312 行、style.css 1053 行水墨宣纸风） |

## 2.2 架构评价

- **数据层是最大结构性隐患**：单 JSON 文件 + 100ms 缓存 + 每次保存整文件重写。已实测复现**丢失更新**：跨缓存 TTL 持有的数据库引用，其写入会被下一次 loadDatabase 返回的新对象覆盖。并发玩家一多必然丢数据，最终需迁 SQLite/D1。
- 优点：分层清晰；231 端点中仅 5 个公开只读端点无鉴权（game.js 的 realms/maps/dungeons/items/guilds）；输入消毒、限流、审计齐备；前后端 152 个 API 路径仅 2 个死条目，契约对齐度高。

## 2.3 已证实的 P0 问题（逐项实测）

| # | 问题 | 证据 | 影响 |
|---|---|---|---|
| 1 | **ID 生成器损坏**：`getNextId()`（database.js:82-90）在缓存过期后读到新对象、`id_counters` 从未初始化 → 每次注册产生 id=1 | 实测注册返回 `userId:1`；game.json 中 **8 个 id=1 的用户/角色**（7 个历史损坏 + 1 个实测复现，已清理） | 所有按 id 的查找（登录/战斗/好友）行为不可预测 |
| 2 | **未知 `/api/*` 返回 200+HTML**：SPA fallback 吞掉 API 404 | 实测 `/api/definitely-not-real` → 200 text/html | 前端把 HTML 当 JSON 解析必报错 |
| 3 | **技能页必崩**：app.js:1546,1697 调 `api.getSkills()`，api.js 中无此方法（正确为 `getAllSkills`） | 全文检索确认 | 「技能心法」页 100% 报错 |
| 4 | **副本战斗全废**：前端调 `POST /dungeon/battle`（api.js:358, app.js:480），后端实际为 `/dungeon/enter` + `/dungeon/sweep` | server.js 挂载点比对 | 副本玩法不可用 |
| 5 | **移动端零导航**：≤768px 侧栏被 CSS 隐藏（style.css:645），底部导航只在 resize 事件里显示（app.js:93-97），首次加载不判定 | 代码路径追踪 | 手机用户无任何导航入口 |
| 6 | **管理端用户详情打不开**：`showAdminUserDetail` 写入不存在的 `#modal-popup`（app.js:3173），实际容器是 `#modal` | DOM 比对 | 功能不可达 |
| 7 | **装备槽位四套命名互不兼容**：validate.js:28,30 / ui.js:47 / app.js:130-137 / 数据各一套（pants vs legs、shoes vs boots、armor/clove 多出） | 交叉比对 | 带 slot 的请求被 400 拒绝；部分槽位永不显示 |

## 2.4 旧审计问题修复验证（2026-09-03 审计报告 → 现状）

| 旧审计声称 | 验证结果 |
|---|---|
| BUG-002 generate 接口免费刷物品 | ✅ **已修**：equipment.js:218-243 按品质扣 50-1500 灵石（pet/gongfa 同类接口同样有价） |
| BUG-004 副本奖励不入库 | ✅ **已修**：dungeon.js:51-63 物品写入 db.inventory |
| BUG-005 VIP 加成为空 | ✅ **已修**：character.js:33-59 addExp 应用 expBonus；combat.js:326-327 应用 spiritStoneBonus |
| BUG-006 Buff 系统缺失 | ✅ **已修**：buff.js 完整实现，shop.js:170 激活、cultivation.js:20 与 combat.js:156-159 读取 |
| BUG-007 仙盟创建无消耗 | ✅ **已修**：guild.js:37-54 扣 1000 灵石 |
| BUG-001 三套元素系统并存 | ⚠️ 部分收敛（combat.js:242-250 有归一化表），但中文/英文元素混用仍在 |
| BUG-003 skills.js 死代码 | ❌ **仍在**：src/data/skills.js（1741 行 ESM）在 CommonJS 项目中从未被加载 |
| 赛季空壳 | ❌ **仍在**：有排行榜定义，无积分获取途径 |

**当前真实完成度约 70%**（旧报告的 55-60% 已过时）。

## 2.5 设计方案落地差距（对照 方案规划/确认版设计方案.md）

| 状态 | 系统 |
|---|---|
| 已落地 | 境界 8 大体系、战斗元素克制、签到、成就、邀请、聊天 WS、商城、VIP 10 级、挂机 250h、采集、洞府、炼器/炼丹/符箓/阵法 |
| 部分落地 | 功法（种子数据 0 条但生成链路可用）、灵宠（0 条）、5 星副本评价、赛季、竞技场 |
| 未落地 | 突破失败惩罚细化（realm.js 现行逻辑与设计不符）、渡劫战斗、套装 10-90%、元素法痕、仙盟战 10v10、擂台赛、三榜分层、宗门流派技能树、交易行 |

其他发现：
- `方案规划/06-美术风格/美术风格.md` 仍写"青色金色紫色"，与实际落地的宣纸水墨风（inkwash.html：米色 #f5f0e8 + 朱红 #c41e3a）脱节，**属过时文档**
- 9 个文档化 UI 页面无一完整实现；「宗门」并入「仙盟」；商贸/设置页严重不足
- 前端死代码：32 个 api.js 方法从未被调用（含整个签到系统入口）、character.js/game.js 未被 index.html 加载、ui.js 有 5 个函数因目标容器不存在静默失效
- XSS 面：app.js 107 处 innerHTML 写入点 escapeHtml 调用为 0（后端消毒只覆盖请求体；WS 聊天完全绕过 Express 中间件）；JWT 存 localStorage 且经 URL 传输
- 数据库其他完整性问题：checkin 集合重复 id、`id_counters` 仅 checkin 一项、28 个 shop 商品引用的 item 部分缺失

## 2.6 九重宫阙建议开发路线

1. **P0 修复日（1 天）**：修 getNextId（按集合最大 id 推导 + 写时校验）、清洗 8 个重复 id、API 404 返回 JSON、技能页方法名、副本战斗路径、移动端导航判定、槽位命名统一
2. **数据迁移（2-3 天）**：game.json → SQLite（better-sqlite3），访问层替换，根治并发与完整性
3. **内容填充（3-4 天）**：功法/灵宠种子数据、突破惩罚与渡劫、5 星评价、赛季积分来源
4. **体验补全**：签到入口、管理弹窗、统一 XSS 转义

---

# 第三部分 · 原版深勘结果（验证员复核版）

## 3.1 ⚠️ `原版/gh-actions/` 是断链孤儿目录（已独立复核）

- 仅 5 文件；`_anti_detect.js` **缺失**，而 auto_farm.js:2 / auto_sell.js:2 均 `require('./_anti_detect')` → 运行必 MODULE_NOT_FOUND
- daily_trial.js:108 调用 `/player/data` —— 服务器无此端点（实际 `/player/state`），属迁移前旧快照
- **维护中的 20 文件集在 `工单系统/gh-actions/`**（含 _anti_detect.js 与 4 个更新版同名脚本）
- **与整理的关系**：4 个游戏工作流在移动前就已断，路径更新未引入回归。修复选项：
  - **A（推荐）**：工作流指向 `工单系统/gh-actions/` fork（一行路径改动 ×4）
  - **B**：复制 _anti_detect.js 进原版并修端点（保持归属清晰）

## 3.2 AI 训练器问题（逐项 SHA256/代码级验证）

- 浏览器插件发布的 DQN 模型是**错误架构旧版**：两份 dqn_model_bot0.json 均为 [50,170,85,35]（SHA256 `0687A30F…536438`），训练器实际构建 [50,512,256,128,35]；dqn_inference.js 不读 layerSizes 所以静默错跑
- `backward()` 把 ReLU 导数用在激活值而非 pre-activation → **梯度本身错误**；PER 半实现；lr 衰减算了不用
- 训练器是全仓唯一用旧签名密钥 `xXaEdLr_s1gn_K3y_2024!@#` 的客户端 → 所有 POST 403，解释了 avgReward -2946 的训练结果
- 元数据 episodes:10 且从未出现正奖励 episode

## 3.3 死代码与技术债

- web-client 五套主题引擎（inkwash/wabi/minimal/glass/brutal.js）**从未被引用**——CSS 加载、JS 死代码
- deploy_cf_temp_email.js TDZ 崩溃（`const fs` 声明在闭包函数之后），且打印明文管理员密码/JWT 密钥
- order-worker（591 行）：纯 HTTP 调度器 + cron `*/30 * * * *`，**wrangler.toml 无任何 D1/KV/DO 绑定**；`арi_keys` 变量名为西里尔同形字（真实存在，非显示伪影）
- browser-plugin：9 个配置控件中 8 个是装饰；popup 关闭决策循环即死
- crawler/stress_test.js 曾对生产 `idlexiuxianzhuan.cn` 发 400 并发变更类 POST（11841 请求 / 2357×429 / 无 5xx）——**需确认授权记录**
- 批量注册工具的反检测体系只有一维真正生效：服务器无条件信任 X-Forwarded-For 首段；machine_id 等 12+ 种伪装全是死重
- 4 个客户端端点路径错误（/exchange/my-listings 等）被 try/catch 静默吞掉

## 3.4 价值定位

原版是**功能最完整的参考实现**：battleEngine 12 万行、exchange 10 万行、仙盟/师徒/联赛等九重宫阙未做系统均有现成逻辑，是九重宫阙功能开发阶段的"答案册"。

---

# 第四部分 · 工单系统勘察要点

## 4.1 三套并行后端

| 位置 | 状态 |
|---|---|
| `工单系统/functions/`（121 文件） | **死代码**——Pages 实际读取的是 pages-frontend/functions/ |
| `工单系统/pages-frontend/functions/`（121 文件） | **实际生效**，115 端点 |
| `工单系统/worker/index.js`（2321 行） | 已废弃单体，全量非 /api 请求 301 跳 pages.dev |

前两者 12 个文件已漂移（_middleware、auth/login、orders/[id]/status 等）。

## 4.2 数据库层

- **25 张 schema 表 + 5 张仅存于迁移的表**；但 `withdrawals`（提现）、`recharge_instant`（即时充值）**全链路无 DDL**，代码在读写 → 功能 100% 坏
- `users.spirit_stones`、`redeem_log.coins` 列不存在；`cs_messages`/`chat_messages`/`recharge_orders` 各有两个不兼容定义（代码用 v15/v13 版）
- deploy.yml 用 20 条 `ALTER TABLE ... || true` 掩盖迁移失败
- `ider-order-system-clean` 是 **2026-08-27 旧快照**（非"干净版"），根目录更新（09-06）；独有价值：`migration_v10_withdrawals.sql`（但列名与现代码不匹配）与 `fix_missing_columns.sql`

## 4.3 安全

- 🔴 API 密钥 `ider-gh-5fc9c4b0899ad14bc2ee55562eaa5b3a` 明文硬编码 19 文件（REVIEW_REPORT.md:141 早已标记未修）；另有第二签名密钥
- 🟠 管理员鉴权 4 种写法混用（isAdmin / is_admin 裸判 / role 判断 / _auth.js），存在权限黑洞
- ✅ 正面：前端 40 个页面模块与磁盘 100% 对应、api.js 全部调用有后端对应

## 4.4 工单修复优先级

1. 轮换两把密钥 → env/Secrets（已进 git 历史，需考虑历史清理）
2. 补 withdrawals/recharge_instant DDL（按现代码列名重写）
3. 废弃 schema.sql 旧定义改纯迁移驱动，解决 3 组表定义冲突
4. 统一管理员鉴权为 _auth.js
5. 删除根 functions/ 与 worker/（消除三副本）
6. 抢救 clean 版两个 SQL 后归档

---

# 第五部分 · 私服简述

Cloudflare 全托管改造（Pages 前端 + Workers API + 聊天服务 + D1 + DO），PLAN.md 完整，前端为 Vue3 无构建 SPA 带皮肤切换器。`deploy-ideer.yml` 三个部署链路完好（整理时已同步路径）。含 5 份子代理未深读的大文件（worker/index.js、game-api db.js 77KB），审计细节以转述为准。

---

# 第六部分 · 跨项目安全红线汇总（按紧急度）

| # | 事项 | 位置 | 状态 |
|---|---|---|---|
| 1 | 请求签名密钥=服务器默认值，散布 19+ 文件，可伪造全部变更接口 HMAC | 原版 server/config.js:75 及全仓 | 🔴 需协同轮换 |
| 2 | ~190 条明文游戏账号凭据入库（accounts.txt 95 有效+55 封号、accounts.md 16KB），.gitignore 缺 *.txt | 原版/批量注册工具/ | 🔴 待处理 |
| 3 | 生产环境 400 并发变更类 POST 压测记录 | 原版/源代码/crawler/ | 🔴 需授权确认 |
| 4 | 工单 API 密钥 + SIGN_KEY 明文 19 文件 | 工单系统/ | 🔴 旧审计已标未修 |
| 5 | gmToolToken: 'qingdeg666222rag' 硬编码 | 原版 | 🟠 |
| 6 | jwtSecret/passwordPepper 默认 'change-me-in-production-use-env' | 原版 server/config.js:48,50 | 🟠 |
| 7 | GET 请求默认跳过签名验证 | 原版 server/middleware/sign.js | 🟠 |
| 8 | 九重宫阙 JWT 存 localStorage + WS URL 传 token + WS 消息无消毒 | 九重宫阙 | 🟠 |

---

# 第七部分 · 待决策事项

1. **4 个游戏工作流**：选项 A（指向工单系统 fork）/ 选项 B（修原版本体）/ 暂不动
2. **整理变更提交**：345 项暂存变更是否 commit
3. **九重宫阙开发起点**：建议从 P0 修复日开始（七个已证实问题均为单点改动）
4. **安全红线处理排期**（第一部分第 2、4 项建议优先）

---

*报告生成：2026-09-11 · 所有 ✅/🔴 标记条目均有代码级或运行时证据；未验证项已在对应章节明示*
