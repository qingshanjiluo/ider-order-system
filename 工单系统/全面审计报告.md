# 全面审计报告

## 已修复 P0 严重 Bug（7个）

### A1: 注册后不返回 token
- **文件**: `functions/api/auth/register.js`
- **原因**: INSERT 用户后不返回 token，前端无法自动登录
- **修复**: 使用 `ins.meta.last_row_id` 获取新用户ID，生成 token 存入 sessions 表并返回

### A2: 申诉提交永远失败
- **文件**: `functions/api/appeals/index.js:21`
- **原因**: 后端校验 `!title`，但前端只发 `{type, content}` 无 title
- **修复**: `const finalTitle = title || type || '申诉'`

### A3: 售后提交永远失败
- **文件**: `functions/api/after-sales/index.js:22`
- **原因**: 同 A2，后端要 title 但前端只发 `{order_id, type, content}`
- **修复**: `const finalTitle = title || type || '售后'`

### A4: 售后列表永远空白
- **文件**: `pages-frontend/src/js/pages/after-sales.js:38`
- **原因**: 前端读 `res.after_sales` 但 API 返回 `{items: results}`
- **修复**: `res.items || res.after_sales || res || []`

### A5: 排行榜 total_spent 单位错位
- **文件**: `pages-frontend/src/js/pages/leaderboard.js:81`
- **原因**: `total_spent` 以修仙币单位存储（见 status.js:36 注释），但前端直接追加 ¥ 符号
- **修复**: `(item.total_spent || 0) / 400` → 正确显示人民币金额

### A6: 邀请页灵石套餐价格错误
- **文件**: `pages-frontend/src/js/pages/invite.js:123,181`
- **原因**: `(p.price / 100000000)` 对合理价格（如 500 万）结果为 0
- **修复**: 直接显示 `${p.price}万灵石`

### A7: ORDER_TYPE_LABEL 缺少新工单类型
- **文件**: `orders.js:7`, `order-detail.js:9`, `dashboard.js:8`, `admin-orders.js:6`
- **原因**: 仙盟采集/试炼测试/每日试炼未加入 LABEL 映射，回退为"购买邀请积分"
- **修复**: 全部四文件统一加入 `'仙盟采集':'仙盟采集', '试炼测试':'试炼测试', '每日试炼':'每日试炼'`

## 已确认无问题项

### invite API 数据不通
- 前端发往 `/api/invite/info` 和 `/api/invite/packages` 的请求由 Worker 处理而非 Pages Functions
- Worker 代码在 `gh-actions/` 或旧版目录中，不在本次审计范围内
- 如果 boost_tiers 显示空白，需检查 Worker 端 invite 路由实现

### 优惠码分钟舍去
- `admin-coupons.js` 使用 `<input type="datetime-local">` 已正确处理时间

### admin-recharge 标题重复
- `renderOrdersTable()` 被两个 tab 各自调用一次，各自有独立 `<thead>`，是标准 tab 模式

### apps.js 双 return
- 项目中无 `apps.js`，仅 `app.js` 且无此 bug

## 待处理项（非本次修复范围）

### 1. Worker 端 invite 路由 boost_tiers 字段验证
- 如果邀请页倍率阶梯仍空白，需要检查 Worker 端 `/api/invite/info` 是否返回 `boost_tiers` 数组

### 2. GH Actions 工作流文件同步
- `gh-actions/` 目录下的扫描/分配脚本与 Worker 和 Functions 代码保持同步
- 建议检查 `scan_orders.js` 中的 `MAX_ACCOUNTS_PER_RUN` 是否配置正确

### 3. 多环境字段不一致
- 部分 API 前端用 `camelCase` 字段，后端 DB 用 `snake_case`，已在多个文件中做转换映射
- 建议统一为后端 snake_case 标准，前端 API 层转换

## 修复文件清单

| # | 文件 | 修复内容 |
|---|------|---------|
| 1 | `functions/api/auth/register.js` | 注册后生成 token 并返回 |
| 2 | `functions/api/appeals/index.js` | title 可选，回退 type |
| 3 | `functions/api/after-sales/index.js` | title 可选，回退 type |
| 4 | `pages-frontend/src/js/pages/after-sales.js` | res.items 替代 res.after_sales |
| 5 | `pages-frontend/src/js/pages/leaderboard.js` | total_spent/400 转人民币 |
| 6 | `pages-frontend/src/js/pages/invite.js` | 万灵石显示 + buyPackage |
| 7 | `pages-frontend/src/js/pages/orders.js` | ORDER_TYPE_LABEL 补充 |
| 8 | `pages-frontend/src/js/pages/order-detail.js` | ORDER_TYPE_LABEL 补充 |
| 9 | `pages-frontend/src/js/pages/dashboard.js` | ORDER_TYPE_LABEL 补充 |
| 10 | `pages-frontend/src/js/pages/admin-orders.js` | ORDER_TYPE_LABEL 补充 |
