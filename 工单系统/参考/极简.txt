<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Less</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --black: #000000;
      --white: #FFFFFF;
      --gray-100: rgba(0, 0, 0, 0.08);
      --gray-400: rgba(0, 0, 0, 0.40);
      --gray-600: rgba(0, 0, 0, 0.60);
    }

    html {
      font-size: 16px;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      font-weight: 400;
      background-color: var(--white);
      color: var(--black);
      line-height: 1.6;
      letter-spacing: -0.01em;
    }

    .page {
      max-width: 680px;
      margin: 0 auto;
      padding: 120px 32px 160px;
    }

    @media (max-width: 640px) {
      .page {
        padding: 80px 24px 100px;
      }
    }

    /* ── 标识 ── */
    .index {
      font-weight: 400;
      font-size: 0.75rem;
      letter-spacing: 0.04em;
      color: var(--gray-400);
      margin-bottom: 80px;
    }

    @media (max-width: 640px) {
      .index {
        margin-bottom: 56px;
      }
    }

    /* ── 主标题 ── */
    .hero {
      margin-bottom: 96px;
    }

    .hero-title {
      font-weight: 300;
      font-size: clamp(3.2rem, 8vw, 5.5rem);
      line-height: 1.05;
      letter-spacing: -0.04em;
      margin-bottom: 32px;
    }

    .hero-lead {
      font-weight: 400;
      font-size: clamp(1rem, 1.6vw, 1.125rem);
      line-height: 1.7;
      color: var(--gray-600);
      max-width: 480px;
    }

    @media (max-width: 640px) {
      .hero {
        margin-bottom: 64px;
      }
    }

    /* ── 分隔 ── */
    .rule {
      width: 100%;
      height: 1px;
      background-color: var(--gray-100);
      margin: 0 0 80px;
    }

    @media (max-width: 640px) {
      .rule {
        margin-bottom: 56px;
      }
    }

    /* ── 原则列表 ── */
    .principles {
      list-style: none;
    }

    .principle {
      margin-bottom: 72px;
    }

    .principle:last-child {
      margin-bottom: 0;
    }

    .principle-header {
      display: flex;
      align-items: baseline;
      gap: 16px;
      margin-bottom: 16px;
    }

    .principle-number {
      font-weight: 400;
      font-size: 0.7rem;
      letter-spacing: 0.06em;
      color: var(--gray-400);
      min-width: 24px;
    }

    .principle-title {
      font-weight: 500;
      font-size: 1.125rem;
      letter-spacing: -0.02em;
      line-height: 1.3;
    }

    .principle-body {
      padding-left: 40px;
      font-weight: 400;
      font-size: 0.9375rem;
      line-height: 1.7;
      color: var(--gray-600);
    }

    @media (max-width: 640px) {
      .principle {
        margin-bottom: 48px;
      }
      .principle-body {
        padding-left: 0;
      }
    }

    /* ── 页脚 ── */
    .footer {
      margin-top: 120px;
      padding-top: 24px;
      border-top: 1px solid var(--gray-100);
      display: flex;
      justify-content: space-between;
      align-items: baseline;
    }

    .footer-left {
      font-weight: 400;
      font-size: 0.75rem;
      color: var(--gray-400);
      letter-spacing: 0.02em;
    }

    .footer-right {
      font-weight: 400;
      font-size: 0.75rem;
      color: var(--gray-400);
      letter-spacing: 0.02em;
    }

    @media (max-width: 640px) {
      .footer {
        margin-top: 80px;
        flex-direction: column;
        gap: 8px;
      }
    }
  </style>
</head>
<body>

  <div class="page">

    <div class="index">001</div>

    <header class="hero">
      <h1 class="hero-title">Less.</h1>
      <p class="hero-lead">
        剔除一切非必要元素，直到无法再减少为止。剩下的，就是信息本身。
      </p>
    </header>

    <div class="rule"></div>

    <ul class="principles">

      <li class="principle">
        <div class="principle-header">
          <span class="principle-number">01</span>
          <h2 class="principle-title">减法优先</h2>
        </div>
        <p class="principle-body">
          每一个进入画面的元素都必须证明其存在的必要性。装饰不是罪过，但未经审视的装饰是。当犹豫是否保留某个元素时，答案通常是否定的。
        </p>
      </li>

      <li class="principle">
        <div class="principle-header">
          <span class="principle-number">02</span>
          <h2 class="principle-title">留白即内容</h2>
        </div>
        <p class="principle-body">
          空白不是缺失，而是最高层级的信息组织方式。它定义了元素之间的关系，创造了视觉的呼吸节奏，让核心信息获得应有的权重。
        </p>
      </li>

      <li class="principle">
        <div class="principle-header">
          <span class="principle-number">03</span>
          <h2 class="principle-title">单一焦点</h2>
        </div>
        <p class="principle-body">
          页面在任一时刻只应存在一个视觉焦点。多重焦点意味着没有焦点。层级通过尺寸、字重和间距建立，而非颜色或动效。
        </p>
      </li>

      <li class="principle">
        <div class="principle-header">
          <span class="principle-number">04</span>
          <h2 class="principle-title">无情绪理性</h2>
        </div>
        <p class="principle-body">
          设计不应传达情绪，而应传达事实。色彩、形状、排版的选择基于功能与逻辑，而非审美偏好。最终呈现的是冷静的、可信赖的秩序。
        </p>
      </li>

    </ul>

    <footer class="footer">
      <span class="footer-left">Inter / 2026</span>
      <span class="footer-right">No. 001</span>
    </footer>

  </div>

</body>
</html>